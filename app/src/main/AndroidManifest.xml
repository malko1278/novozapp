<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.novozapp.fam.activities">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:icon="@mipmap/logo_novoz"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_novoz_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.NovozApp">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" /> <!-- The API key for Google Maps-based APIs. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/maps_api_key" />

        <!-- <activity android:name=".MainMapActivity"> -->
        <activity android:name=".MainMapActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MonumentActivity"
            android:theme="@style/Theme.NovozApp.NoActionBar" />
        <activity
            android:name=".CityHistoryActivity"
            android:theme="@style/Theme.NovozApp.NoActionBar" />
        <activity
            android:name=".ItemMonumentActivity"
            android:theme="@style/Theme.NovozApp.NoActionBar" />
    </application>

</manifest>